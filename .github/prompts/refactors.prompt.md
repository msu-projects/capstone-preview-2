---
agent: agent
---

Currently i am refactoring the system. You should treat the types in the src/types/index.ts as the truth. The files that currently depends on src/types/index.ts and produces type errors should be treated obsolute, don't mind them for now.
